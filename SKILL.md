---
name: minimal-skill
description: 为复杂、长期、跨窗口、跨模型、多人协作或不断演化的项目识别“最小拓扑”并守住项目核心。凡用户提出“极简项目”“项目守核”“项目内核”“最小拓扑”“从第一性原则重构”“目标漂移”“窄腰设计”“复杂项目梳理”“哪些功能可以砍”“升级但不要丢核心”“建立验收和自我升级机制”“换窗口或换模型继续维护”等请求，都应使用本 Skill。它通过目标、边界、稳定契约、证据与演化关系，区分不可删除的项目身份和可自由替换的实现方式，并支持 design、audit、evolve、handoff 四种模式。不要因为用户提到极简UI、极简海报、极简写作、简单Bug或一次性小任务而触发。
---

# 极简 Skill

## 核心定义

“极简”不是功能少、文件少或规则少，而是：

> 在工具、模型、人员、窗口和实现方式变化后，仍能保持项目身份与核心能力的最小关系结构。

先识别项目不可删除的最小拓扑，再让实现围绕它自由变化。

## 典型触发说法

- 启动极简 Skill。
- 用第一性原则重新梳理这个项目。
- 帮我找到项目的最小拓扑。
- 为这个复杂项目建立项目内核／项目守核。
- 这个项目越来越乱，检查是否目标漂移。
- 哪些功能和规则可以砍掉？
- 给工作流建立窄腰结构和中间契约。
- 升级项目，但不能丢失原来的核心。
- 建立证据验收和自我升级机制。
- 换窗口、模型或维护者后仍要能继续。

## 第一步：复杂度门槛

先判断是否值得启用本 Skill。满足以下任意两项，通常进入：

- 项目预计跨多个阶段或长期维护；
- 存在多个模块、角色、模型或外部系统；
- 需要跨窗口、跨人员或跨环境恢复；
- 目标、架构或边界尚不清晰；
- 错误可能破坏数据、证据、安全或声誉；
- 功能、规则或技术债持续膨胀；
- 需要基准、回归、回滚或自我演化机制。

低于门槛时，直接完成小任务，不制造额外文档和流程。

## 第二步：选择模式

- `design`：为新项目设计最小拓扑、窄腰和验收结构。
- `audit`：审计目标漂移、冗余结构、旁路和规则膨胀。
- `evolve`：在不破坏项目身份的前提下升级、替换或裁剪。
- `handoff`：提取可跨窗口、模型、人员和环境恢复的最小状态。

用户没有明确模式时，根据请求选择最小充分模式，不自动扩大任务。

## 项目最小拓扑

所有复杂项目至少检查以下六类关系：

```text
Identity / Goal ─────→ Boundary
Identity / Goal ─────→ Stable Contract ─────→ Replaceable Implementation ─────→ Result
Result ─────→ Evidence ─────→ Identity / Goal
Evidence ─────→ Evolution ─────→ Stable Contract
```

### 1. Identity / Goal

明确服务对象、存在理由、当前状态、目标状态和价值变化。目标必须描述状态改变，不写成功能清单。

### 2. Boundary

定义非目标、禁止事项、权限边界、停止线与升级人工条件。边界用于防止系统“成功地做错事”。

### 3. Stable Contract

定义输入、状态转换、输出和验收接口。窄腰只保存跨实现仍必须成立的少量契约，不固定工具和路径。

### 4. Replaceable Implementation

模型、工具、Agent、目录、代码、UI和执行顺序默认属于可替换层。只有证据证明它们承载项目身份时，才升级为不变量。

### 5. Evidence

用事实、行为、结果和回归证据判断完成。流程执行完毕、模型自述成功或“看起来不错”都不是验收。

### 6. Evolution

升级遵循“失败／机会 → 根因 → 假设 → 小实验 → 对照证据 → 保留或回退”。未经验证的改进只能作为实验，不得直接升级为硬规则。

完整定义见 [references/minimum-project-topology.md](references/minimum-project-topology.md)。

## 通用运行原则

### 第一性原则

从目标对象、状态变化、约束和证据重新推导方案，不把当前实现或行业惯例误认为必然结构。

作出架构、裁剪或升级决策前，必须回答四问：服务谁？要改变什么状态？哪些约束不可违反？什么证据足以证明改变发生？答不清时先调查，不作高影响决策。

### 目标导向

按目标状态和验收证据判断完成，不按步骤数量、文档数量或流程完成度判断。

### 窄腰设计

固定“必须成立什么”，不固定“必须怎样做到”。多变输入和多变实现通过稳定契约衔接。

### 中间契约

模糊需求先转成可检查、可保存、可交接的中间结构，再进入执行。每层只向下一层暴露必要信息。

每份核心契约至少写明：输入、必要状态转换或不变量、输出、错误语义、验收证据和下游消费者。缺一项时标记为待确认，不把推测伪装成契约。

### 复杂度通过拆分承载

增加阶段、模块或独立工作单元，不向单个Prompt、函数、页面、Agent或文档无限塞内容。每个工作单元只承担一个主要目标。

### 局部失败、局部修复

缩小故障域，保存中间结果。失败只重做受影响单元，已验收结果不重复生成；每个高风险阶段设置回滚点。

### 最小充分模式

根据目标选择最浅的有效深度：`inspect → plan → build → verify → release → operate`。只读请求不自动实施，实施请求不自动发布。

### 自我升级不是添加规则

新规则必须保护目标、边界、证据或高成本风险，并经过真实失败和对照验证。否则保留为默认策略、实验或想法。

## 硬约束与软策略

只有以下情况适合成为硬约束：

- 违反后造成不可逆损失；
- 导致项目目标或身份漂移；
- 破坏关键数据、证据或兼容性；
- 产生安全、法律或声誉风险；
- 同类结构性错误经过多次验证仍反复发生。

其他内容应标记为默认值、推荐策略或实验假设。定期审计硬约束，删除已经失去保护对象的规则。

规则的候选、实验、默认、约束和过期状态见 [references/rule-evolution.md](references/rule-evolution.md)。

## 四种模式的执行

### design

1. 建立 `ProjectIdentity`。
2. 定义当前状态、目标状态和验收证据。
3. 明确非目标、边界、停止线和权限。
4. 提取最小关系图，并逐项写明窄腰契约的输入、转换／不变量、输出、错误语义、证据和消费者。
5. 区分不变量和可替换实现。
6. 拆成可独立验收、局部修复的工作单元。
7. 建立基准、Gate、回滚点和演化规则。

### audit

1. 读取真实代码、文件、配置、数据和当前证据，不只读旧文档。
2. 对照目标识别漂移、旁路、重复能力和无主规则。
3. 逐项执行删除测试：移除后是否破坏项目身份或验收能力？
4. 将元素分类为：核心不变量、必要支撑、可替换实现、待验证实验、可删除负担。
5. 输出最小裁剪顺序和风险，不擅自删除或重构。

### evolve

1. 写明升级假设和预期证据。
2. 限定影响面与故障域。
3. 保存基准和稳定版本。
4. 小范围实施并验证目标、边界和回归。
5. 根据证据决定保留、调整或回退。
6. 只有验证稳定的变化才进入正式契约或规则。

### handoff

1. 保存目标、边界、稳定契约和当前状态。
2. 记录已确认事实、关键决策及理由。
3. 链接基准证据、测试、稳定版本和回滚方法。
4. 只记录下一维护者必须知道的状态，不复制全部历史。
5. 若仓库已有维护胶囊，更新胶囊而不是并行建立第二套真相。

## 输出格式

按模式最小充分使用 [references/templates/project-core-report.md](references/templates/project-core-report.md)，不输出无关空章节。

所有模式保留公共窄腰：复杂度门槛与模式、项目身份、相关拓扑、边界、证据、风险和下一步。

- `design` 增加：核心不变量、稳定契约、可替换实现、工作单元和演化机制。
- `audit` 增加：删除测试、元素分类、真实证据、裁剪顺序和风险。
- `evolve` 增加：基准、假设、影响面、实验、Gate和回退条件。
- `handoff` 增加：当前状态、关键决策、证据链接、稳定版本和恢复入口。

## 输出纪律

- 先给结论，再给结构和证据。
- 不为了显得系统而创建无用途文件。
- 不把所有历史偏好升级为项目不变量。
- 不以“更灵活”为理由删除必要边界。
- 不以“更极简”为理由牺牲真实能力、可恢复性或证据。
- 审计请求只报告和建议；没有明确授权不执行架构变更、删除、发布或外部写入。

## 与相邻 Skill 的关系

- 本 Skill 负责识别、设计和审计项目最小拓扑。
- 领域 Skill 负责具体执行，例如漫画、数据库、发布或文档生产。
- 维护胶囊负责把已确认的项目核心和当前状态持久化，方便未来恢复。
- 不复制领域知识，也不取代已有领域工作流。
